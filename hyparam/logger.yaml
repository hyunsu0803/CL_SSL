wandb:
  wandb_ok: True
  init:
    project: cl_ssl
    entity: 
    name: 
    config:
      sigma:
      snr:
      with_coherent_noise:
      coherent_noise_snr:
      num_speaker:
    
optimize_method: 'min'

save_csv: /root/clssl/results/logger/log.csv
png_dir: /root/clssl/results/logger/loss.png

model_save_dir: /root/clssl/results/model_checkpoint/
out_txt:
  dir: /root/clssl/results/logger/exp.txt