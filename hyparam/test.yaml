# torch_start_method: 'fork'
torch_start_method: 'spawn'
randomseed: 0


resolution: 1
acc_threshold: 5
local_maximum_distance: 30

SCL: True
trained_scl_model_path: /root/clssl/results_scl_221628/model_checkpoint/best_model.tar   # for scl

model: /root/clssl/results/model_checkpoint/best_model.tar

result_folder: 
  room_type: ['evaluation']

  inference_folder: '/root/clssl/results/'
  


GPGPU:
  device_ids: [0]