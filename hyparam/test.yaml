torch_start_method: 'spawn'
randomseed: 0


resolution: 1
acc_threshold: 20
local_maximum_distance: 20

SCL: True
finetune: True

model_for_finetune: ./results/model_checkpoint/best_mae_model.tar

result_folder: 
  room_type: ['evaluation']

  inference_folder: './results/'
  


GPGPU:
  device_ids: [0]