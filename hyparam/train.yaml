torch_start_method: 'spawn'
randomseed: 0

acc_threshold: 20
local_maximum_distance: 20

last_epoch: 150
resume_epoch: 0

GPGPU:
  device_ids: [0]

SCL: True
finetune: False
# finetune : False
trained_scl_model_path: ./results_basic_cl/model_checkpoint/last_model.tar   # for initial scl+doa training
# finetune : True
model_for_finetune: ./results/model_checkpoint/last_model.tar  # only for fine-tuning
