torch_start_method: 'spawn'
randomseed: 0

acc_threshold: 20
local_maximum_distance: 20

last_epoch: 400
resume_epoch: 0

GPGPU:
  device_ids: [0]

SCL: True
finetune: True
# finetune : False
trained_scl_model_path: ./results_scl_p-gru_333/model_checkpoint/best_model.tar   # for initial scl+doa training
# finetune : True
model_for_finetune: ./results/model_checkpoint/last_model.tar  # only for fine-tuning
